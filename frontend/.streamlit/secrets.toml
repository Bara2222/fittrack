# Optional Streamlit secrets for local/dev use
# You can set api_base to point to backend: e.g. api_base = "http://backend:5000/api"
# For now keep safe defaults to avoid missing-file warning
api_base = "http://backend:5000/api"

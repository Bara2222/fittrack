# Streamlit secrets for local development
# Do NOT commit real credentials.

# Frontend config
app_version = "v2.0"
api_base = "http://localhost:5000/api"

# OAuth placeholders (if referenced later)
GOOGLE_CLIENT_ID = ""
GOOGLE_CLIENT_SECRET = ""
